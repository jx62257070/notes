1.let、const
2.解构赋值
    用途:函数参数、返回值   class的解构赋值，对象初始化的时候赋值为对象属性
			    例如：let config={
			          name:'xiaoli',
                                  sex:'male'
                                  }
                                  let xiaoli = new Human(config);//config为对象，该句也可写成let xiaoli = new Human({name:'xiaoli',sex:'male'});
3.字符串扩展
    includes()、startsWith()、endsWidth()、repeat()、字符串模版
4.函数扩展
    默认参数、rest参数、箭头函数
5.数组扩展
    扩展运算（反rest）、find()、findIndex()、includes()
6.对象的扩展
    Object.assign()
7.Promise

8.class

9.module

10.练习